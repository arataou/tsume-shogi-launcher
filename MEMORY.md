# 协作记忆

- 用户的开发方式是多个窗口或任务共用同一个本地工作区目录。
- 后续每次任务开始前，先检查当前分支、`git status --short --branch`、`git worktree list --porcelain`、相关文件状态，并先刷新 `origin/main`；把检查结论告知用户。
- 如果发现可能由其他窗口产生的重叠改动，先说明风险和范围，等待用户确认后再编辑或提交。
- 不擅自回滚、清理、stash、覆盖或删除已有改动；需要处理时先取得明确授权。
- 小功能和范围明确的小缺陷修复优先使用一个基于最新 `origin/main` 的独立 worktree，完成修改、针对性验证、提交、推送、创建 PR 并合并到 `main`；不要在 PR 前先合并本地 `main`，也不要为了交付重复创建 worktree。
- 小变更的非关键实现细节采用最小可回退默认，不因每个细节反复打断流程；只有影响范围、兼容性、安全或验收的选择才请求用户决定。
- 提交前检查 staged diff，只提交确认过的范围；远端流程遵循 `AGENTS.md`，不得因为用户没有在当前消息中重复提到推送或合并而跳过。
