# 协作记忆

- 用户的开发方式是多个窗口或任务共用同一个本地工作区目录。
- 后续每次任务开始前，先检查当前分支、工作区状态、Git worktree 列表，以及相关文件是否存在其他窗口的未提交改动，并把检查结果告知用户。
- 如果发现可能由其他窗口产生的重叠改动，先说明风险和范围，等待用户确认后再编辑或提交。
- 不擅自回滚、清理、stash、覆盖或删除已有改动；需要处理时先取得明确授权。
- 实现新功能时优先创建独立 Git worktree，在其中完成修改、验证和提交；不要直接在多个窗口共用的工作区开展新的功能开发。
- 提交前检查 staged diff，只提交确认过的范围；除非用户明确要求，不执行远端推送。
