# 仓库协作规则

## 多窗口共用工作区（重要）

- 用户会在多个窗口或任务中共用同一个本地工作区目录，不使用独立 worktree 隔离修改。
- 每次开始开发、调试、提交或切换目标前，必须先检查并向用户说明：当前分支、`git status --short --branch`、`git worktree list`，以及本次相关文件是否已有未提交改动。
- 不得假设工作区干净，也不得把当前文件中的所有改动默认视为本窗口产生的改动。
- 如果相关文件已有未提交改动，或检查结果表明可能有其他窗口正在写入，必须先说明影响范围并让用户确认，再继续编辑或提交。
- 不得擅自执行 `git reset`、`git checkout`、清理、stash、覆盖或删除操作来处理其他窗口的改动；除非用户明确授权。
- 修改时尽量使用精确补丁并保留已有改动；提交前再次检查工作区和 staged diff，确认提交范围后再提交。
- 实现新功能时，优先先创建独立的 Git worktree，在独立 worktree 中修改、验证并提交；不要直接在多个窗口共用的主工作区开始新功能。
- 仅创建或使用分支不能隔离同一目录中的文件修改；需要真正隔离时，必须使用独立 worktree 或独立项目副本，并先征得用户同意。
- 用户未明确要求时不要推送远端。
