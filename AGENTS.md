# 仓库协作规则

## 自动交付（重要，必须执行）

- 对用户明确提出的小功能，以及范围明确的小缺陷修复，完成开发和验证后必须自动完成完整交付链：提交专用分支 → 推送远端 → 创建 Pull Request → 合并到 `main`。
- 这是默认工作流程，不得因为用户没有在当前消息中再次单独提到“推送”“PR”或“合并”而停在本地提交；也不得只报告提交哈希就结束。
- 只有遇到权限不足、推送失败、合并冲突、检查失败，或流程中出现必须由用户决定的事项时，才在安全位置暂停并说明具体阻塞原因。
- 合并完成后必须向用户报告分支、提交、PR、合并结果和 worktree 路径；除非用户明确要求，不自动删除原始 worktree。

## 多窗口协作与 worktree（重要）

### 目录、分支与归属

- 用户会在多个窗口或任务中协作开发。新功能、较大修复、重构和需要持续调试的任务，默认使用独立 Git worktree，避免直接在共享主工作区修改。
- 共享主工作区是仓库根目录，主要用于只读检查、跨分支整合和用户明确要求的小型文档修改；不得在其中同时开展多个相互独立的开发任务。
- 所有辅助 worktree 统一放在仓库根目录的 `.worktrees/<task-slug>/` 下。除非用户明确指定，不在仓库外或其他自定义目录创建 worktree；`.worktrees/` 只用于存放 Git worktree，不放普通文件。
- 每个 worktree 使用独立分支；创建新分支时默认使用 `codex/<task-slug>` 前缀，除非用户指定已有分支或明确要求其他名称。不得让多个 worktree 同时绑定同一个分支。
- `.worktrees/` 已纳入仓库忽略规则，其中的目录和文件不属于主工作区待提交内容。

### 状态确认与播报

- 每次开始开发、调试、提交或切换目标前，必须检查当前分支、`git status --short --branch`、`git worktree list --porcelain`，以及本次相关文件是否已有未提交改动；向用户说明检查结论即可，无需逐行粘贴原始输出。
- `git worktree list --porcelain` 是 worktree 登记状态的唯一依据。位于 `.worktrees/` 且已登记、分支唯一、路径存在的 worktree 属于预期状态，不得把它描述为“未跟踪改动”“异常 worktree”，也不得仅因它存在而反复要求确认。
- 常规状态播报只需说明当前分支、相关文件是否有未提交改动，以及当前任务涉及的 worktree；只有在创建、切换、交接、清理 worktree，或发现异常时，才列出完整 worktree 清单。
- 需要单独提示的异常包括：`.worktrees/` 之外的辅助 worktree、目录存在但未被 Git 登记、登记项缺失或路径不存在、分支重复绑定、锁定/可修剪状态，或相关文件可能正被其他窗口修改。
- 不得假设工作区干净，也不得把现有改动默认视为本窗口产生的改动。若相关文件已有未提交改动，或检查结果表明可能有其他窗口正在写入，必须先说明影响范围并让用户确认，再继续编辑或提交。

### 创建、修改与清理

- 创建 worktree 前，先确认目标目录不存在且目标分支没有被其他 worktree 使用；优先使用 `git worktree add .worktrees/<task-slug> ...`。目标目录已存在但未登记时，不得覆盖、删除或复用，先报告并等待处理。
- 修改时尽量使用精确补丁并保留已有改动；提交前再次检查工作区和 staged diff，确认提交范围后再提交。
- 不得擅自执行 `git reset`、`git checkout`、清理、stash、覆盖、删除 worktree、删除分支或 `git worktree prune` 等操作来处理其他窗口的改动；除非用户明确授权。
- 对用户明确提出的小功能，完成验证后自动提交对应分支、推送远端、创建 Pull Request 并合并到 `main`；遇到权限、冲突、检查失败或其他需要用户决策的情况时暂停并说明。合并后向用户说明分支、提交、PR 和 worktree 路径，不自动删除原始 worktree。
- 不同任务切换时，优先使用新的 worktree 或新的会话，避免在同一 worktree 中混入多个目标的改动。

## UI Design Rules

- 明确禁止“屎黄色 + 古典字体 + SaaS 卡片味”等视觉组合。
- 保持现代桌面应用风格，不使用暖黄色、金色、米色作为主色。
- 不使用 serif 字体，使用系统 sans-serif 字体。
- 避免过度圆角、渐变、浮夸阴影和营销网站式卡片设计。
- 优先高信息密度、清晰层级与紧凑间距。
- 风格参考 Windows 11、Linear、VS Code、GitHub Desktop，而不是 SaaS landing page。
- 只调整视觉样式，不改变现有交互结构和信息架构。
